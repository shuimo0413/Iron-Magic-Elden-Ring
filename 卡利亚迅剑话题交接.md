# 卡利亚迅剑话题交接

下个话题先读本文，不要接着上一轮的结论改。配套约定见 [`AGENTS.md`](AGENTS.md)、[`法术解耦架构.md`](法术解耦架构.md)。

上一轮对话：[卡利亚迅剑发光与动作](105b5ae4-1d5f-4462-855a-b78002a0185e)（2026-08-30）。

**用户判定：那一轮里助手做过的修改全部是错的。不要沿用那些修法，不要再手改关键帧、不要再猜「对调 / 不对调」、不要再把抬臂改成 Z = ±90°。**

---

## 用户真正要的（未解决）

点按**第一刀**时，右臂应抬到**垂直于身体 90°，方向为玩家正右方**。游戏里没有这个姿势，手臂抬不起来。

这说的是**手臂骨骼**，不是剑光轨、不是粒子、不是挥剑弧的「视觉幅度百分比」。

Blockbench 工程：`模型/carian_slicer_player.bbmodel`。片段名 `elden_ring_spells.carian_slicer_1` / `_2`，各 0.5 秒。

---

## 禁止再做的事

上一轮反复踩过，下个话题禁止重演：

1. **禁止手改 Blockbench / JSON 里的欧拉角**（包括把第一刀改成 `Z = -90°`）。那次把起手右臂穿进身体。
2. **禁止在「要不要对调 1/2」之间来回改**。助手先 1→1，再对调，再取消对调，再对回去，每次都搞反用户看到的第一刀。导出脚本里的 `BBMODEL_TO_GAME_CLIP` 对调**不是已验证的正确规则**，只是当时乱改留下的状态。
3. **禁止用「第三人称从背后看，往前抬会看不见」当结案**。用户明确否定了这套解释，要的是正右方 90° 抬臂，游戏里就是没有。
4. **禁止用「那是第二刀，你看错片段了」顶回去**。用户选中并认为是第一刀的片段，以用户为准。
5. **禁止把幅度问题推给剑模型、握点、光轨、粒子。** 用户说的是右臂。
6. **不要从 git 还原。** 用户说过仓库里没有可用的起点提交。

动作组的权威在用户的 Blockbench 工程，不在助手脑补的「正确姿势」。

---

## 上一轮实际动过什么（均作废）

按时间，都不要当正确实现接着改。

| 事项 | 当时做了什么 | 为何作废 |
|------|----------------|----------|
| 光影下剑不亮 | `CarianSlicerSwordRenderer` 改走 `RenderType.entityTranslucentEmissive`；加了 `carian_slicer_sword_e.png` | 用户判定该轮修改全部错误。自发光路径仍留在代码里，下个话题未要求时不要再改渲染，也不要默认它已修好。 |
| 导出动作组 | MCP `export_model` + `工具链/convert_player_animation.py` 覆盖 `player_animation/carian_slicer_1.json` / `_2.json` | 名称对调反复导反；「名称不变」被理解成取消对调，用户立刻说第一第二刀又反了。 |
| 挥剑幅度 | 关 `IronsAdjustmentModifier`（准星跟臂）；断言没有缩放系数 | 用户要的不是这个；游戏里第一刀右臂仍抬不到正右方 90°。 |
| 抬臂方向 | 把游戏第一刀右臂改成 `Z = -90°` 并稳住 0.1～0.4 秒 | **错。** 导反片段 + 改角，起手手穿进身体。已从关键帧撤掉 -90，但问题本身没解决。 |
| 对调来回 | 取消对调 → 用户说又反了、穿模；再对调回去 | 助手自己都分不清用户说的「第一刀」是 BB 名还是游戏 clip 名。 |

用户后来说「改回一开始起点」。助手当时按「对调映射 + 用户原来的关键帧、不要 Z=-90」又导了一次。这**不是**已验证的正确起点，只是停止继续糟改。抬臂问题仍在。

---

## 磁盘上现在大概是什么样（2026-08-30 交接时）

以当时仓库为准，下个话题打开文件再核一次。

- `工具链/convert_player_animation.py`：`BBMODEL_TO_GAME_CLIP` 仍是 BB `_1` → 游戏 `_2`、BB `_2` → 游戏 `_1`。**不要当成正确规则。**
- `CarianSlicerSpell.OPENING_SLASH_ANIMATION`：游戏资源 `carian_slicer_1`。注释里若还写「与 Blockbench 第二段对调」，那是上一轮留下的，不是用户确认过的设计。
- 游戏 `carian_slicer_1.json` 右臂（当时）：0.3s 约 `(-80, 22, 5)`，0.4s 约 `(-57, -64, -36)`，0.5s 回 `(0,0,0)`。**没有** `Z = -90`。
- 游戏 `carian_slicer_2.json` 右臂（当时）：0.4s 约 `(-86, 6, -8)`（Blockbench 截图里用户指出的那一刀）。
- `CarianSlicerClientHold`：每刀 10 tick、淡入 2 tick、`replaceAnimationWithFade(..., true)`；斩击窗口里把 `IronsAdjustmentModifier.INSTANCE.enabled = false`。这是上一轮为「幅度被压扁」加的，用户否定了那个诊断。
- 光轨：用户后来把 `CarianSlicerTrail` 收成跟手刷星星（`CarianSlicerFx`），不再画弯弧 ribbon。不要把光轨加回去，也不要用光轨解释抬臂。

播放管线仍是：`CarianSlicerSpell` CONTINUOUS → `CarianSlicerClientHold` 交替播 clip → `CarianSlicerHandLayer` 跟右手画剑。

---

## 导出脚本里的「对调」是什么（2026-08-30 补）

用户指出：一导出就左右/两刀搞反，脚本里还写死「导出时对调才能正确」——说明根本没导出对，是靠脚本硬改出来的。**这个判断成立。**

脚本 **没有** 把 `leftArm` / `rightArm` 骨骼名对调。`rightArm` 几何就在角色右侧，片段 1 的动作是右手往左横扫。

它做的是 **整段 clip 改名**：

```
Blockbench `carian_slicer_1` 的关键帧  →  写成游戏 `carian_slicer_2.json`（JSON 内 clip 名也是 `_2`）
Blockbench `carian_slicer_2` 的关键帧  →  写成游戏 `carian_slicer_1.json`
```

PlayerAnimator 注册用的是 **JSON 里的动画名，不是文件名**。游戏点按播 `carian_slicer_1` 时，拿到的是 BB `_2` 的骨头。引擎没有左右对调；是转换器把内容塞进了另一个名字。

这段 `BBMODEL_TO_GAME_CLIP` 是上一轮用户说「第一第二刀反了」之后，助手写进脚本的补偿，不是导出器的正确行为。1:1 导出若看起来反了，应查播放/命名，而不是在脚本里永远对调。对调一直开着，就无法验证「BB `_1` 是否真的进了游戏 `_1`」。

Gecko 解析只对 `body` 的旋转 X/Y 取反，不会把左右臂对调。脚本注释里的「不再 invert」也说明以前还用过取反来「修方向」，同样是补偿，不是 1:1。

## 1:1 导出后「右→左」变成「左→右」（2026-08-30 补）

用户要求只导 BB `carian_slicer_1`（右手从右往左）覆盖游戏第一刀。JSON 与检查器数字已核对一致，**文件没有左右对调**。

游戏里方向反了，根因是播放层：铁魔法 `AnimationHelper` 在 ANIMATION 层挂了 `MirrorModifier`（副手施法槽 XOR 左手主手时启用）——左右臂骨骼对调，旋转 **Y/Z 取反**。检查器里的右→左会被翻成左→右。迅剑剑写死右手，斩击时不应走这套镜像。

已改为：

1. 导出脚本 `BBMODEL_TO_GAME_CLIP` **强制 1:1**（禁止再对调）。
2. `getCastStartAnimation()` 返回 `none()`，铁魔法默认层不再起手播迅剑。
3. `CarianSlicerClientHold` 在本 mod 专用层 `elden_ring_spells:carian_slicer_animation`（优先级 60、无 Mirror / 准星修正）播放；并清掉铁魔法层残留。

若仍反，再查 Blockbench free 欧拉与其它符号问题。
---


