# 法术解耦架构（构建说明书）

下个话题按本文落地，不要再开一轮架构讨论。配套说明见 `AGENTS.md`。

**状态：** Spell 根目录只放 `XxxSpell`；被调用的函数按职责进 `helper/` `curve/` `combat/` `fx/` `data/`。玩法数字仍由 `EldenRingServerConfig.apply()` 写 Spell 字段。

---

## 不做什么

- 不按法术族拆 Java 包（不要 `content/carian` 这类目录）。复杂咒的 Curve / Combat / Fx 按**职责**分子包，不按卡利亚 / 海摩拆。
- 不拆 Gradle 多模块，不自研平行魔法系统。
- 冷却 / 最大等级 / 法术开关不进本模组 toml，走铁魔法 JSON。
- 颜色、粒子密度、彗星头缩放、握点偏移不进配置。

---

## 整合包改数值：不用重新编译

玩法数字已经走 NeoForge 配置。改游戏实例文件即可，不必 `gradlew build`。解耦只砍 Tuning 中间层，不改变这件事。

| 要改什么 | 文件 | 重编译？ |
|----------|------|----------|
| 伤害、蓝耗、弹速、转向角、流星数量、爆炸/扇形半径、穿透、雨云寿命等 | `config/elden_ring_spells-server.toml` | 否 |
| 矿洞密度、Y 范围、表面概率 | `config/elden_ring_spells-common.toml` | 否（新区块才看得出） |
| 冷却、最大等级、启用/禁用、铁魔法侧蓝耗/法强倍率 | `config/irons_spellbooks_spell_config/elden_ring_spells/<法术id>.json` | 否 |
| 剑颜色、握点、彗星头缩放、挥砍滚转、粒子密度 | Java 写死 | 要 |

- 单人世界若已生成过，实际生效常是 `saves/<世界>/serverconfig/elden_ring_spells-server.toml`。
- 专用服改服务端 toml。铁魔法 JSON 没有时：`/ironsSpellbooks generate_file elden_ring_spells:glintstone_pebble full`。
- 改完重进世界即可。正在飞的弹不会中途改伤害。

---

## 目标项目结构

单模块、单 jar。法术相关类全部进 `spell/`。删除 `tuning/`。

```
src/main/java/com/eldenring/spells/
  EldenRingSpellsMod.java              # @Mod：注册 config + DeferredRegister
  EldenRingSpellsClient.java           # 只转发到下面三个注册类

  config/                              # 玩法数字的唯一默认值 + toml 绑定
    EldenRingConfigs.java
    EldenRingServerConfig.java         # elden_ring_spells-server.toml
    EldenRingCommonConfig.java         # 矿洞
    ConfigSpecHelper.java

  spell/                               # 打开这里应只看到 XxxSpell
    EldenRingAbstractSpell.java
    SpellBookStatReloader.java
    XxxSpell.java                      # ISS 胶水 + config 运行时字段
    helper/                            # 出手清障、持续咒锁人/补刷实体
    curve/                             # 复杂咒时间轴（写死）
    combat/                            # 复杂咒命中（读 Spell 字段）
    fx/                                # 复杂咒粒子/音效（写死）
    data/                              # CONTINUOUS：MagicData 里绑实体

  entity/                              # 薄 tick：问曲线 → 打人 → 特效
    AbstractGlintstoneProjectile.java
    GlintstoneVisualStyle.java         # 从 client Drawer 提出；实体禁止 import client.render
    *Projectile.java / *Entity.java

  client/
    ClientParticleProviders.java
    ClientEntityRenderers.java
    ClientItemModels.java
    CarianSlicerClientHold.java
    CometAzurClientLock.java
    render/                            # carian / haima / glintstone 视觉子目录可留

  particle/  registry/  world/  worldgen/  sigil/  mixin/
```

复杂咒磁盘文件（迅剑）：`CarianSlicerSpell` + `curve/CastCurve` + `combat/Combat` + `fx/Fx` + `data/CastData` + `entity/CarianSlicerEntity` + Hold + Renderer。不建 Tuning。

简单魔砾：`GlintstonePebbleSpell` + Projectile + `helper/GlintstoneCastHelper`，不必拆 Curve/Combat/Fx。

启动注册顺序保持：Configs → Attributes → Schools → Effects → Blocks → Items → Features → Particles → Entities → Spells → CreativeTabs。

---

## 施法调用顺序

铁魔法负责按键、扣蓝、冷却、动画。本模组不接施法键。`onCast` 双端都会进，刷实体必须 `!level.isClientSide`；末尾必须 `super.onCast`。

### 0. 进世界

```
EldenRingConfigs.onModConfig
  → EldenRingServerConfig.apply()        写 XxxSpell 运行时字段
  → SpellBookStatReloader.reloadAll()   写 AbstractSpell.baseManaCost 等
```

逻辑读 `XxxSpell` 字段，不读 toml `.get()`，不读 Tuning。

### 1. 按下施法键

```
铁魔法查 XxxSpell.getCastType()
  INSTANT     → onCast
  LONG        → onServerCastTick 蓄力 → onCast
  CONTINUOUS  → onServerPreCast → onServerCastTick + 每约 10 tick onCast（扣蓝）
              → 松手/没蓝/到时 onServerCastComplete
客户端 Hold（仅 CONTINUOUS）：CarianSlicerClientHold / CometAzurClientLock 发 CancelCast
```

### 2. INSTANT（魔砾 / 大槌 / 辉剑 / 流星 / 创星雨）

```
XxxSpell.onCast
  服务端: 用 Spell 字段算伤害
         简单弹 → helper.GlintstoneCastHelper.spawnAlongLook
         近战   → new XxxEntity
         齐射   → new GlintstoneStarVolleyEntity
  双端: super.onCast

XxxEntity.tick / AbstractGlintstoneProjectile.tick
  → XxxCastCurve.progress / isHitWindow / pose
  → 伤害窗: XxxCombat.resolve
  → 客户端: XxxFx
Renderer 只读 CastCurve / 同步字段
```

### 3. CONTINUOUS（迅剑 / 亚兹勒）

```
onServerPreCast      → new Entity, CastData.bind, refreshWhileCasting
onServerCastTick     → 补刷实体, refreshWhileCasting, 回写当前伤害
onCast               → 只 super.onCast（扣蓝），不要再 new 实体
onServerCastComplete → entity.requestStop
Entity.tick          → Curve → Combat → Fx → 淡出 discard
```

### 4. 对照表

| 流程 | 入口 | 生成 | 每 tick | 伤害 | 特效 |
|------|------|------|---------|------|------|
| 魔砾线 | `onCast` | CastHelper | 弹道基类 | 命中回调 `DamageSources` | 基类 trail + 写死彗星头 |
| 流星齐射 | `onCast` | StarVolleyEntity | Volley 再调 CastHelper | 同魔砾 | 同魔砾 |
| 迅剑/大槌 | PreCast 或 onCast | XxxEntity | Entity → Curve → Combat → Fx | XxxCombat | XxxFx |
| 亚兹勒 | PreCast + CastTick | CometAzurJetEntity | 喷流判定 | Combat | CometAzurFx |

---

## 数字放哪

**玩法（进 toml）：** `EldenRingServerConfig.defineInRange` 是唯一默认值；`apply()` 写到 `XxxSpell` 运行时字段。

每道咒共用：`base_mana_cost` / `mana_cost_per_level` / `base_spell_power` / `spell_power_per_level` / `cast_time_ticks`。

再按类型：

| 类型 | 额外 toml 键 |
|------|----------------|
| 单发追踪弹 | 伤害系数、飞行速度、索敌半径、每 tick 最大转向角；有爆炸再加半径 |
| 连发流星 | 上面 + `projectile_count`；错峰间隔可留 |
| 旋飞 | 追踪套件 + 伤害系数 + `max_entity_hits` |
| 创星雨 | 伤害系数、每 tick 雨针、下落速度、结算间隔、雨云半径/寿命 |
| 魔法之境 | 半径、持续、每级额外持续、法强加成 |
| 亚兹勒 | 蓄力时长、喷流射程/半径/间隔、伤害系数 |
| 大槌 | 直击/冲击波伤害系数与半径、两种击退 |
| 迅剑 | 伤害系数、扇形半径/半角、单刀周期、击退 |
| 辉剑 | 伤害系数、悬停时长、飞行速度、索敌半径、转向角 |

矿洞 common.toml：噪声阈值、Y 范围、囊半径、表面/簇概率、每 chunk 上限。

**写死（进对应类，不进 toml，不留 Tuning）：**

- 视觉：`COMET_HEAD_*`、TrailStyle、粒子强度、ARGB、剑锤缩放
- 跟手：`GRIP_*`、辉剑 HOVER_RIGHT/UP、bob
- 动画：迅剑 HIT_TICK/淡出/滚转；大槌 IMPACT_TICK / SWING_START；亚兹勒螺线公式
- 弹道实现：出生前移、追踪延迟、索敌锥角、epsilon、命中 inflate
- 索敌打分权重 → `AbstractGlintstoneProjectile`
- 冷却与最大等级种子写在 Spell 的 `DefaultConfig`；运行时以 ISS JSON 为准

应从现有 toml 删掉的例子：`projectile_spawn_forward_offset_blocks`、`tracking_start_delay`、`acquire_cone`、`hit_inflation`、`slash_fade_ticks`、`hit_tick`、`slash_vertical_half_height`、`swing_start_tick`、`impact_tick`、`entity_lifetime_ticks`、`spiral_orbit_radius`、`overhead_star_count`、整节 `glintstone_homing`。

---

## 复杂咒职责

| 类 | 数字从哪来 |
|----|------------|
| XxxSpell | config 写回的运行时字段；ISS 生命周期，不塞清障/锁人/扇形判定 |
| curve/XxxCastCurve | 写死的时间轴 / 姿态 |
| combat/XxxCombat | 读 Spell 的伤害/半径；`applyDamage` |
| fx/XxxFx | 写死的粒子/音效/镜头震 |
| helper/* | 出手清障、持续咒锁人补刷 |
| data/XxxCastData | 这次吟唱绑着哪颗实体 |
| XxxEntity | 薄 tick |

实体禁止 `import com.eldenring.spells.client.render...`。`visualStyle()` 抽到 `entity.GlintstoneVisualStyle`。`GlintstoneFx` 不再调用创星雨/毁灭流星。`GlintstoneColor` 不 import `ModItems`。

---

## 落地顺序（下个话题按此做）

1. 按 KEEP 表瘦身 `EldenRingServerConfig`；`apply` 改为写 Spell 字段（可先仍写 Tuning，再迁）。
2. HARDCODE 项搬进 Entity / Renderer / Curve / Fx；删除 `tuning/`；修到能 `compileJava`。
3. 拆 `EldenRingSpellsClient` → `ClientParticleProviders` / `ClientEntityRenderers` / `ClientItemModels`。
4. 样板：卡利亚迅剑抽出 CastCurve / Combat / Fx。
5. 样板：海摩大槌同样三件套。
6. `GlintstoneVisualStyle` 外提；瘦身 `GlintstoneFx`。

成功标准：整合包改伤害只动 toml；改剑颜色不碰 toml；源码搜不到 `XxxTuning`；实体不 import `client.render`。
