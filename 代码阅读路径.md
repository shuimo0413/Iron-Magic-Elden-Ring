# 代码阅读路径（顺着读）

这份不是架构说明书。架构看 [`法术解耦架构.md`](./法术解耦架构.md)，工程约定看 [`AGENTS.md`](./AGENTS.md)。  
**这篇只告诉你：IDE 里从哪个文件点进去，下一份打开谁，读到哪算这条线走完。**

先记住一件事：这模组**不接施法键**。按键、扣蓝、冷却、动画是铁魔法的活。你们这边只填「出手之后干什么」。所以不要从 `KeyMapping`、输入事件开始翻，那边基本没有你们的逻辑。

建议用 IDE 的「转到声明」往下点，别在资源管理器里乱点。每个步骤标了**必读方法**，其它字段注释可以扫一眼，不必逐行抠。

---

## 整条路长什么样

```
启动挂载
  → 进世界写数字
  → 铁魔法喊 onCast
  → Spell 刷实体
  → 实体 tick 干活
  → 客户端把实体画出来
```

现有法术其实就四类走法。把**魔砾那条**走通，其它三条都是加戏：

| 类型 | 代表 | 比魔砾多什么 |
|------|------|----------------|
| 瞬发单发弹 | 辉石魔砾 | 无，这就是样板 |
| 瞬发连发 | 辉石流星 | 中间多一个「齐射调度员」 |
| 瞬发近战 / 时序 | 海摩大槌、创星雨、辉剑 | 实体自己有时间轴 |
| 按住持续 | 卡利亚迅剑、彗星亚兹勒 | PreCast / CastTick / 松手 |

---

## 第 0 步：先别打开这些

刚上手时跳过，否则会被细节淹没：

- `tuning/` —— 旧数字仓库，待删，读了会和 toml 打架
- `工具链/` —— 离线画贴图的，跟运行时无关
- `particle/**` 里每个粒子类 —— 先当「Fx 会喊它们」即可
- mixin、世界生成、矿物方块 —— 跟施法主线无关
- 铁魔法反编译源码 —— 等你们自己的回调看懂了再对照

---

## 第 1 步：模组怎么挂上去

**打开** `src/main/java/com/eldenring/spells/EldenRingSpellsMod.java`

从上往下读完就行，很短。看构造函数那一串 `register`：**Configs → Attributes → Schools → Effects → Blocks → Items → Features → Particles → Entities → Spells → CreativeTabs**。方块必须在物品前面，因为水晶簇的 BlockItem 依赖方块。

然后打开旁边的客户端入口：

**打开** `EldenRingSpellsClient.java`

它自己几乎不干活，只转发三件事：粒子 Provider、实体 Renderer、卷轴模型。记一下这三个类名，后面画弹的时候会回来。

**打开** `registry/ModSpells.java`

扫一遍字段名就够：这里是「游戏里有哪些咒」的目录。点哪个 Spell 类都从这儿能跳进去。具体施法逻辑不在这里。

读完这一步你该能回答：游戏启动时，法术是怎么被铁魔法看见的。

---

## 第 2 步：数字什么时候写进法术

构造 Spell 时写的是代码默认值。toml 是**进存档才加载**的，所以有回写。

**打开** `config/EldenRingConfigs.java`  
看 `register` 和 `onModConfig`：server.toml 来了就 `EldenRingServerConfig.apply()`，再 `SpellBookStatReloader.reloadAll()`。

**打开** `config/EldenRingServerConfig.java` 的 `apply()`（大约 182 行起）  
不用把 900 行 toml 定义全读完。看它怎么把伤害系数、弹速、转向角写到 `GlintstonePebbleSpell.SPELL_DAMAGE_PER_SPELL_POWER` 这种静态字段上。**运行时逻辑读的是这些字段，不是 `.get()`。**

**打开** `spell/SpellBookStatReloader.java`  
它再把蓝耗 / 法强 / 吟唱 tick 写回铁魔法 `AbstractSpell` 的 protected 字段。冷却仍走铁魔法 JSON，这里不动。

可选扫一眼：`spell/EldenRingAbstractSpell.java`，就一个 `applyBookStats`，知道为啥要有这层薄基类就行。

读完这一步你该能回答：我改 toml 为什么不用重编译，但要重进世界。

---

## 第 3 步：顺着一道咒读完（最重要）

从这里开始才是「施法」。**只跟辉石魔砾**，别同时开迅魔砾、大魔砾，那些几乎是换皮。

### 3.1 法术胶水

**打开** `spell/package-info.java`  
三分钟读完。生命周期就三条：铁魔法按 CastType 分流、`onCast` 双端都会进、末尾必须 `super.onCast`。

**打开** `spell/GlintstonePebbleSpell.java`

建议读的顺序：

1. 类注释（这道咒是什么）
2. 上面那一大坨静态字段：玩法（弹速、转向角、伤害系数）和视觉（彗星头缩放）是分开的
3. 构造函数：把默认值塞进铁魔法字段
4. `getCastType()` → `INSTANT`，按下就出手
5. **`onCast`**（文件末尾）：服务端刷法阵 + 叫 Helper 生成弹，然后 `super.onCast`

看到 `GlintstoneCastHelper.spawnAlongLook(...)` 就跟进去，不要在 Spell 里找飞行逻辑，没有。

### 3.2 弹是怎么生出来的

**打开** `spell/helper/GlintstoneCastHelper.java`

精读 `spawnAlongLook` 那个九参数重载。它在干一件很土但很关键的事：贴墙、低头、站方块边时，裸 `addFreshEntity` 会让弹体嵌进实心块，下一 tick 撞没了，体感就是「出手即没」。所以它会：眼睛沿视线前移 → 碰到墙收回 → 按碰撞箱再挪 → 写入方向和伤害 → 刷实体 → 可选出手闪光。

`clockwiseRegularPolygonOffset` 这步**先跳过**，那是流星圆阵用的，第 4 步才需要。

### 3.3 弹体子类其实很薄

**打开** `entity/GlintstonePebbleProjectile.java`

整份都可以读，但你会发现它几乎只在 **override 一堆 getter**，数字全指向 `GlintstonePebbleSpell` 的静态字段。真正飞、转、撞在基类里。

跟进 `trailStyle()` / `visualStyle()` 看一眼返回啥，知道「实体不 import 客户端渲染包」是怎么做到的。

### 3.4 飞行、追踪、命中

**打开** `entity/AbstractGlintstoneProjectile.java`

这是弹道线最厚的一份，**按方法读，别从上到下抠完 600 行**。

必读顺序：

1. 类注释：找不到目标不会删弹，只会直飞；「飞一会就没」几乎总是撞方块
2. `tick()` —— 坐标非法就 discard，然后 `super.tick()`（铁魔法弹道基类会接着走飞行 / 命中）
3. `handleHitDetection()` —— 前 4 tick 宽限期不撞；之后射线打实体再打方块
4. `handleEntityHoming()` —— 延迟几 tick 才开始转；锥外不锁；每 tick 最多转那么多度
5. `onHitEntity` / `onHitBlock` —— 伤害和爆裂粒子从这儿出去

拖尾粒子、历史点缓冲扫一眼即可，画光带的细节第 3.5 步再说。

到这里，**服务端「按下 → 出手 → 飞 → 打到人」已经闭环。**

### 3.5 客户端怎么把弹画出来

回到第 1 步记住的转发：

**打开** `client/ClientEntityRenderers.java`  
找到 `GLINTSTONE_PEBBLE` 挂的是 `GlintstoneProjectileRenderer`。

**打开** `client/render/glintstone/GlintstoneProjectileRenderer.java`  
`render()`：先光轨，再彗星头。数字来自实体的 `visualStyle()` / `trailStyle()`。

有兴趣再点进：

- `GlintstoneCometHeadDrawer` —— 立体晶核 + 光晕
- `GlintstoneTrailRenderer` —— 曲线光带（原理另见 `辉石曲线拖尾原理.md`，主线不必现在读）

粒子侧扫一眼 `particle/glintstone/GlintstoneFx.java` 的方法名（`castBurst`、拖尾、命中），具体粒子类可以先不进。

读完第 3 步你该能回答：我按一下魔砾，从 Spell 到 Helper 到实体到 Renderer，每一跳是谁。

---

## 第 4 步：连发只多一个调度员

魔砾读懂了，迅魔砾 / 大魔砾 / 辉石彗星 / 帚星**不用逐份精读**。打开任意一个 `onCast`，确认还是 `GlintstoneCastHelper.spawnAlongLook` + 另一个 Projectile 子类，就可以合上。

真正多出来的是齐射。

**打开** `spell/GlintstoneStarsSpell.java`  
只看 `onCast`：它**不**循环刷三发，只 `new GlintstoneStarVolleyEntity`。注释里写了为啥要清无敌帧。

**打开** `entity/GlintstoneStarVolleyEntity.java`  
精读 `tick()` 和 `spawnNextProjectile()`。自己不可见、不碰撞，每 `staggerTicks` 在视线圆阵顶点叫一次 Helper。发完 `discard`。

这里才回去看 Helper 的 `clockwiseRegularPolygonOffset`。

流星雨、毁灭流星就是换 `VolleyKind` 和数量，Spell 可以扫 `onCast` 一眼就过。

读完第 4 步你该能回答：为啥不能在 `onCast` 里 for 循环打三发。

---

## 第 5 步：复杂咒怎么拆（近战样板）

弹道线走完，换「实体自己有时间轴」的。先读大槌，它是 INSTANT，没有按住那套干扰。

按这个顺序打开，每份都很短：

1. `spell/GavelOfHaimaSpell.java` —— `onCast` 里 `new GavelOfHaimaEntity`，把直击伤害和冲击波伤害传进去
2. `spell/curve/GavelOfHaimaCastCurve.java` —— 第几 tick 跟手、第几 tick 砸地、握点偏移。**视觉/动画写死在这儿**
3. `spell/combat/GavelOfHaimaCombat.java` —— 真正打人，读 Spell 上的半径和伤害
4. `spell/fx/GavelOfHaimaFx.java` —— 粒子音效
5. `entity/GavelOfHaimaEntity.java` 的 `tick()` —— 薄调度：问曲线 → 到点 Combat → Fx
6. 想看锤子怎么跟手，再打开 `client/render/haima/HaimaGavelRenderer.java`，它只读 Curve，不打人

读完你会发现一个固定句式：**Spell 刷实体，Entity.tick 问 Curve，打人走 Combat，好看走 Fx。** 迅剑、亚兹勒也是这句式，只是生命周期更长。

---

## 第 6 步：按住不放（CONTINUOUS）

铁魔法 CONTINUOUS 默认会喷到时间或蓝耗尽，而且蓄力条、起手动画都按「持续吟唱」来。迅剑要看起来像近战连斩，所以你们自己补了客户端松手。

**打开** `spell/CarianSlicerSpell.java`  
按方法读，不要只看 `onCast`（它几乎是空的，只 `super` 扣蓝）：

1. `onServerPreCast` —— 起手立刻刷剑，绑进 `CarianSlicerCastData`
2. `onServerCastTick` —— 每 tick 告诉剑「还按着」；剑丢了就补刷
3. `onCast` —— **禁止再 new 实体**
4. `onServerCastComplete` —— `requestStop`，当前这刀收完再淡出

**打开** `entity/CarianSlicerEntity.java` 的 `tick()`  
跟手 → 伤害窗里 `CarianSlicerCombat.resolve` → 周期到了看还按不按 → 接下刀或淡出。

然后才读三件套：`spell/curve/CarianSlicerCastCurve` / `combat` / `fx`。Curve 里的 HIT_TICK、淡出、握点和大槌一个意思。

**打开** `client/CarianSlicerClientHold.java`  
看 `ClientTickEvent`：本地玩家在放迅剑、键抬起来了，就发铁魔法的 `CancelCastPacket`。另外藏蓄力条、每一刀重播左右挥砍。没有这份，松手剑还会自己挥到蓝耗尽。

亚兹勒可以紧接着对照着读，不必当全新东西：

1. `spell/CometAzurSpell.java`：生命周期回调。下落判定 / 锁人 / 补刷喷流在 `spell/helper/CometAzurCasting`
2. `entity/CometAzurJetEntity.java` 的 `tick()`：贴在锁定的喷口上，按间隔打圆柱
3. 客户端松手在 `client/CometAzurClientLock.java`（和迅剑 Hold 同一类职责）

读完第 6 步你该能回答：为啥迅剑的 `onCast` 是空的，真正砍人的是谁。

---

## 第 7 步：剩下几道，扫 onCast 就知道往哪跳

不必再精读每一份 Spell 字段。打开 `onCast`（或 PreCast），看它 `new` 了谁：

| 法术 | 打开 | 看出手之后谁接 |
|------|------|----------------|
| 魔法辉剑 | `MagicGlintbladeSpell.onCast` | `MagicGlintbladeEntity`：先悬停，到点再当追踪弹飞出 |
| 创星雨 | `FoundingRainOfStarsSpell.onCast` | `FoundingRainOfStarsEntity.tick`：抽光点 → 亮雨云 → 落雨针。雨针是 `FoundingRainDropEntity`，不走彗星弹 |
| 魔法之境 | `TerraMagicaSpell` | 唯一 `CastType.LONG`。蓄满后刷 `TerraMagicaZoneEntity`，站进去加全局法强 |
| 旋飞魔砾 | `SpiralShardSpell.onCast` | 仍走 CastHelper，但弹体 `SpiralShardProjectile` 内部绕双螺旋 |

辉剑实体的 `tick()` 值得读一下（悬停时把速度清零，避免朝向被拧没），创星雨实体按 `maybeLaunchAscentMotes` / `maybeSpawnOverheadCloud` / `maybeSpawnRainDrops` 三个 maybe 往下看就行。

世界生成、三色水晶、物品卷轴：跟战斗主线无关。想看矿洞再去 `worldgen/GlintstoneCaveFeature.java` 和 `EldenRingCommonConfig`。

---

## 读到哪里算「主线走完」

你能不翻文档、自己用话讲清下面这几句，就可以停：

1. 启动时 Spell 挂到铁魔法注册表，进世界才把 toml 写进静态字段。
2. 魔砾：`onCast` → Helper 清障生成 → 基类限角追踪 → 命中结算 → Renderer 画彗星头。
3. 流星：Spell 只刷 Volley，Volley 再错峰叫 Helper。
4. 大槌 / 迅剑：Entity.tick 问 Curve，Combat 打人，Fx 好看。
5. CONTINUOUS 的 `onCast` 只扣蓝；实体在 PreCast 刷出来，松手靠客户端 CancelCast。

之后再读粒子实现、拖尾数学、Blockbench 模型，都是「把已经懂的那一跳画得更漂亮」，不是另一套系统。

---

## 一张对照表：文件和职责

读的时候若走丢了，用这张表把自己拽回来。

```
EldenRingSpellsMod          启动，注册一串 DeferredRegister
EldenRingSpellsClient       客户端三转发
ModSpells                   法术目录，不是逻辑
EldenRingServerConfig       玩法数字默认值 + apply 写回 Spell
SpellBookStatReloader       蓝耗/法强/吟唱写回铁魔法

XxxSpell                    ISS 胶水：何时刷实体、伤害从哪来
spell/helper                辉石弹出手清障、亚兹勒锁人补刷
AbstractGlintstoneProjectile 飞、转、撞
Glintstone*Projectile       薄：把 Spell 字段填进基类

spell/curve / combat / fx   复杂咒时间轴 / 打人 / 特效
spell/data                  CONTINUOUS 这次吟唱绑哪颗实体
XxxEntity.tick              薄调度
XxxClientHold / ClientLock  CONTINUOUS 松手、藏条、重播动画

ClientEntityRenderers       哪个实体用哪个 Renderer
GlintstoneProjectileRenderer 通用彗星头 + 光带
```

顺藤摸瓜时记住：**Spell 不飞，Entity 不画（服务端），Renderer 不打人。**
