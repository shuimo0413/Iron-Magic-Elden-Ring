package com.eldenring.spells.item;

/** Per-cast state attached to Iron's MagicData, cleared with its native casting state. */
public interface AzurCastCostData {
    double eldenRingSpells$getAzurMultiplier();
}
